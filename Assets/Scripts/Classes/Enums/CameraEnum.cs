namespace Shard.Enums
{
    public static class CameraEnum
    {
        public enum VirtualCamera {
            MAIN,
            ZOOM_IN,
            ZOOM_OUT,
            LEFT_PIVOT,
            TOP_PIVOT,
            TOP_PIVOT_ZOOM_OUT
        }
    }
}
