namespace Shard.Enums
{
    public static class MechanismEnum
    {
        public enum DoorState {
            OPEN,
            CLOSE
        }

        public enum ButtonType {
            PRESS,
            PRESS_RELEASE
        }
    }
}